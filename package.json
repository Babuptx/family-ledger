{
  "name": "family-ledger",
  "version": "3.1.0",
  "description": "A local-first, browser-only family ledger with portable JSON backups.",
  "private": true,
  "license": "PolyForm-Noncommercial-1.0.0",
  "scripts": {
    "test": "node --test",
    "test:core": "node --test tests/ledger-core.test.cjs",
    "check": "npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
